FROM postgres:17-alpine

# Note: pg_cron installation is complex for PostgreSQL 17
# The migration will check if pg_cron is available and use it if possible
# If not available, game ticks will need to be run manually or via external scheduler